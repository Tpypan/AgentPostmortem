# Minimal Example

This shows the minimum config to enable the opencode-postmortem plugin in any project.

1. Copy `opencode.json` to your project root (or add the plugin entry to your existing config).
2. Run `npx postmortem-init` to install command and skill templates.
3. Restart OpenCode.

For full install instructions, see [INSTALL.md](../../INSTALL.md).
